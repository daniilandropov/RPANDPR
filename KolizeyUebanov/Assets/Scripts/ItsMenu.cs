using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItsMenu : MonoBehaviour
{
    public string MenuName;

    protected MenuWorker _menuWorker;

    public void SetMenuWorker(MenuWorker menuWorker)
    {
        _menuWorker = menuWorker;
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
